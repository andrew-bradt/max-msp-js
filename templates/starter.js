const {
  ARGS
} = require('constants');

const {
  createMaxObject,
  createMaxObjects,
  connect,
  cleanup
} = require('utils');

const generateObjects = () => {
  const maxObjects = {
  };

  return maxObjects;
}

const connectMaxObjects = (maxObjects) => {
}

// DELETE AND MOVE TO TRIGGER_RAND.JS

